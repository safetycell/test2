<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shokacshoes">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" /> <!-- Bluetoothデバイスのスキャン機能を使用するためのパーミッションとして、位置情報のパーミッションがAndroid 6.0から必要になった -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ShokacShoes">
        <activity android:name=".DisplayShoes">
        <!--<activity android:name=".step1">-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".step2"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".step3"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".step4"
            android:label="@string/app_name"></activity>
        <activity
            android:name="login.ui.login.LoginActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".resister"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".WarmingUp"
            android:label="@string/app_name"></activity>
        <activity
            android:name="bluetoothcommunicator.MainActivity"
            android:label="@string/app_name" ></activity>
        <activity
            android:name="bluetoothcommunicator.DeviceListActivity"
            android:label="@string/app_name" ></activity>
        <!--<activity
            android:name=".DisplayShoes"
            android:label="@string/app_name" ></activity>-->
        <activity
            android:name=".ViewGraph"
            android:label="@string/app_name" ></activity>


    </application>

</manifest>